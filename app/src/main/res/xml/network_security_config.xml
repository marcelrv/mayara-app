<?xml version="1.0" encoding="utf-8"?>
<!--
  Network Security Config for Mayara.

  Cleartext HTTP is permitted ONLY for the two loopback addresses used by the
  embedded mayara-server:
    - 127.0.0.1  — physical device (JNI server binds here)
    - 10.0.2.2   — Android emulator host loopback alias

  All other traffic (remote network mode) must use HTTPS.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
